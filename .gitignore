# LINEのトークンが入っているので絶対にコミットしない
.env

# 実行時に自動生成されるもの
rain_alert.log
__pycache__/
charts/
.DS_Store

# 注意: rain_alert_state.json は「前回いつ通知したか」を覚えておくファイル。
# GitHub Actionsは毎回まっさらな環境で動くため、これをコミットしておかないと
# クールダウンが効かず、同じ雨で何度も通知が飛んでしまう。だから除外しない。
